//Modules

//CommonJS - every file is a module
//Modules - Encapsulated code (only share minimum)
const john = 'john'
const peter = 'peter'
const sayHi =(name) => {
console.log(`hello ${name}`)
}

sayHi('jigyasa')
sayHi(peter);
sayHi(john)